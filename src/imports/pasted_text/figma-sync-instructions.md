URGENT — SOURCE OF TRUTH IS FIGMA MAKE

This is NOT a redesign task.

The current Figma Make project is the CORRECT and AUTHORITATIVE version of the product.

The GitHub Pages version is OUTDATED / INCORRECT and must be synchronized with the current Figma Make source.

Published URL:
https://uxteambehsazan-commits.github.io/team-arena/

==================================================
PRIMARY RULE
==================================================

FIGMA MAKE = SOURCE OF TRUTH

GitHub Pages = DEPLOYED VERSION THAT MUST BE UPDATED

DO NOT modify the current Figma Make user flow to match GitHub.

DO NOT copy the GitHub flow into Figma.

DO NOT redesign Figma based on the currently published GitHub version.

Instead, use the CURRENT Figma Make project as the definitive reference for:

- User journey
- Routes
- Screens
- Navigation
- Game selection
- Game setup
- Lobby
- Player states
- Game states
- Interactions
- Modals
- Buttons
- Components
- Responsive behavior
- Mobile behavior
- Visual design
- Game logic
- Screen transitions

==================================================
THE CURRENT PROBLEM
==================================================

The GitHub Pages deployment currently shows an older/different user journey.

For example, the published GitHub version currently reaches a Team Arena game screen that looks like the attached screenshot.

However, the CURRENT Figma Make project contains the correct and newer user journey.

Therefore:

DO NOT "fix" Figma to look like GitHub.

Instead, the objective is to make the DEPLOYED / PUBLISHED VERSION reflect the CURRENT FIGMA MAKE VERSION.

==================================================
STEP 1 — AUDIT FIGMA MAKE
==================================================

Before making any changes:

Analyze the ENTIRE current Figma Make project.

Identify:

1. All screens
2. All routes
3. All navigation paths
4. All game-selection states
5. All player-selection states
6. All lobby states
7. All game states
8. All modals
9. All overlays
10. All buttons and interactions
11. All back/exit behaviors
12. All responsive/mobile states

Treat the current Figma implementation as the latest approved product definition.

==================================================
STEP 2 — BUILD THE AUTHORITATIVE FLOW MAP
==================================================

Create an internal flow map based ONLY on the current Figma Make implementation.

Example structure:

ENTRY
↓
SCREEN A
↓
SCREEN B
↓
SCREEN C
↓
GAME SELECTION
↓
GAME SETUP
↓
LOBBY
↓
GAME
↓
GAME STATES
↓
END / RESULT

The exact screens and sequence must come from the CURRENT Figma Make project.

DO NOT infer or invent missing steps.

==================================================
STEP 3 — IDENTIFY THE DEPLOYMENT MISMATCH
==================================================

Compare the current Figma Make implementation against the currently published GitHub Pages version.

The purpose of this comparison is NOT to change Figma.

The purpose is to identify which parts of the GitHub deployment are outdated.

Create an internal mismatch list such as:

- outdated route
- outdated screen
- outdated component
- outdated game flow
- outdated navigation
- outdated interaction
- missing screen
- missing state
- obsolete screen
- obsolete interaction

==================================================
STEP 4 — UPDATE THE IMPLEMENTATION SOURCE
==================================================

The implementation that is published to GitHub must reproduce the CURRENT Figma Make experience.

The following priority must ALWAYS apply:

1. Current Figma Make
2. Current Figma Make interactions
3. Current Figma Make navigation
4. Current Figma Make visual system
5. GitHub Pages only as a diagnostic reference

GitHub must NEVER override Figma.

==================================================
STEP 5 — CRITICAL: DO NOT CREATE A SECOND FLOW
==================================================

There must be ONE canonical Team Arena user journey.

Do not keep:

OLD GITHUB FLOW
+
NEW FIGMA FLOW

as two separate experiences.

Replace/synchronize the outdated implementation so that the deployed experience follows the Figma Make flow.

==================================================
STEP 6 — PRESERVE THE CURRENT FIGMA DESIGN
==================================================

Do not redesign the current Figma screens.

Do not simplify them.

Do not create an alternative UX.

Do not change the current navigation just because the GitHub version behaves differently.

The goal is implementation/deployment synchronization.

==================================================
STEP 7 — VERIFY EVERY ROUTE
==================================================

After synchronization, verify the entire flow from the first screen.

For every interaction check:

- Does the correct screen open?
- Is the correct state preserved?
- Does Back work correctly?
- Does Exit work correctly?
- Does game selection work correctly?
- Does player selection work correctly?
- Does the lobby work correctly?
- Does the game start from the correct state?
- Are all transitions consistent?
- Are mobile layouts consistent?

==================================================
STEP 8 — FINAL VALIDATION
==================================================

The task is COMPLETE only when:

✓ Figma Make remains unchanged as the source of truth.

✓ The published GitHub implementation follows the current Figma flow.

✓ The GitHub version no longer follows the obsolete flow shown in the attached screenshot.

✓ All current Figma screens exist in the deployed implementation where applicable.

✓ All current Figma interactions work.

✓ All current Figma navigation paths work.

✓ No obsolete GitHub-only flow remains.

✓ No duplicate user journey exists.

✓ No dead-end navigation exists.

✓ Mobile behavior matches the current Figma implementation.

==================================================
VERY IMPORTANT
==================================================

DO NOT solve this by modifying Figma to match GitHub.

The correct direction is:

CURRENT FIGMA MAKE
       ↓
SOURCE OF TRUTH
       ↓
IMPLEMENTATION
       ↓
GITHUB
       ↓
PUBLISHED PRODUCT

NOT:

GITHUB
       ↓
Figma Make

==================================================
FINAL INSTRUCTION
==================================================

Before changing anything, first determine WHY the current GitHub deployment does not represent the current Figma Make source.

Possible causes include:

- old source code
- old build
- old deployment
- wrong branch
- wrong commit
- wrong deployment target
- incomplete synchronization
- missing routes
- stale assets
- outdated implementation

Identify the mismatch first.

Then synchronize the deployed implementation with the CURRENT FIGMA MAKE SOURCE.

Do not alter the Figma Make source of truth merely to match the outdated GitHub deployment.